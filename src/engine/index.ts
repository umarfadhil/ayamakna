// =============================================================================
// AyaMakna Semantic Engine — Top-level barrel export
// =============================================================================
// Layer A: Linguistic Engine (morphology, tokenization, root extraction)
// Layer B: Semantic Engine (root clustering, contrast, action, similarity)
// Layer C: Visualization Engine (graph types, config)
// =============================================================================

export * as linguistic from './linguistic';
export * as semantic from './semantic';
export * as visualization from './visualization';
